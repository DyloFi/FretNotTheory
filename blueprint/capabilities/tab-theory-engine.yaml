capability: tab-theory-engine
name: Tab & Theory Engine Developer
owns: >
  Low-level AlphaTab integration, note traversal, note styling
  (NoteStyle), audio/playback state, and mathematical music theory
  helper functions (scale/triad/interval membership). (Ported from
  .agents/agent-team.md, confirmed 2026-07-06.)
never_touches: >
  Must never touch app/page.tsx or modify visual styles, general layout,
  or CSS outside the tab canvas wrapper.
trigger: >
  Task involves AlphaTab rendering, note-coloring algorithms, or music
  theory calculation logic.
consult_before: >
  blueprint/state/knowledge-index.json for prior research on rendering
  approach (see guitar-tab-rendering brief) before re-litigating the
  AlphaTab-vs-VexFlow-vs-OSMD decision; that question is already Decided.
verification: >
  CRITIQUE confirms app/page.tsx and layout/CSS outside the tab canvas
  wrapper were not touched.
